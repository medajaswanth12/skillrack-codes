#include <bits/stdc++.h>
 
using namespace std;

int main(int argc, char** argv)
{
    char a[1000];
    int b;
    cin>>a;
    b=strlen(a);
    for(int i=1;i<=b;i++)
    {
        if(b%i==0)
        {
            cout<<a[i-1];
        }
    }
    return 0;
}
