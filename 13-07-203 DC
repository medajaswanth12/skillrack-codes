#include <bits/stdc++.h>
 
using namespace std;

int main(int argc, char** argv)
{
    int s,e;
    cin>>s>>e;
    for(;s<=e;s++)
    {
        int temp=s;
        int u=temp%10;
        temp/=10;
        int h=temp%10;
        if((u+h)%2==0)
        {
            cout<<s<<" ";
        }
    }
}
